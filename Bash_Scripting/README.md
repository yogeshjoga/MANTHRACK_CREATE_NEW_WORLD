# This is the Bash scripting lang
```BASH
        very popular scripting lang to automate linux commands 
        linux tasks, make very quick projects by usnig BASH Scripting 
        very easy to learn
        no need to install software it needs to be Terminal
        Run with terminal, consume less space in your system
        very easy syntax
        
```